##########ARCHIVO DE CONFIGURACION DE I3##########

##########GENERAL##########

#TECLA MOD
set $mod Mod4

#FUENTE
font pango:Roboto Mono 10

#EJECUTAR AL INICIO
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id xfce4-power-manager
exec_always feh --bg-scale /home/jose/Imágenes/dark.jpg
exec_always picom
exec autodisplay

#CERRAR 
bindsym $mod+Shift+q kill

#RECARGAR i3
bindsym $mod+Shift+r restart

#CERRAR SESION
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m '¿Quieres salir de i3?.' -B 'Si, salir de i3' 'i3-msg exit'"

#RECARGAR CONFIGURACION
bindsym $mod+Shift+c reload

##########MOVER##########

#MODO DE REDIMENSIONAR VENTANAS
bindsym $mod+r mode "Redimensionar"

#REDIMENSIONAR
mode "Redimensionar" {
        #REDIMNESIONAR VENTANAS
        bindsym Left resize shrink  width 10 px or 10 ppt
        bindsym Down resize grow    height 10 px or 10 ppt
        bindsym Up resize shrink    height 10 px or 10 ppt
        bindsym Right resize grow   width 10 px or 10 ppt

        #REGRESAR
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

#MOVER VENTANA FLOTANTE
floating_modifier $mod

#CAMBIAR ENTRE VENTANAS
bindsym $mod+Left  focus left
bindsym $mod+Down  focus down
bindsym $mod+Up    focus up
bindsym $mod+Right focus right

#CAMBIAR POSICION DE LAS VENTANAS
bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Down  move down
bindsym $mod+Shift+Up    move up
bindsym $mod+Shift+Right move right

#ABRIR VENTANAS EN HORIZANTAL
bindsym $mod+h split h

#ABRIR VENTANAS EN VERTICAL
bindsym $mod+v split v

#PANTALLA COMPLETA
bindsym $mod+f fullscreen toggle

#CAMBIAR DISPOSICION DE LAS VENTANAS
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

#CAMBIAR / REGRESAR A VENTANA FLOTANTE
bindsym $mod+Shift+space floating toggle

##########ESCRITORIOS##########

#ASIGNAR ESCRITORIOS
set $ws1  "  1  "
set $ws2  "  2  "
set $ws3  "  3  "
set $ws4  "  4  "
set $ws5  "  5  "
set $ws6  "  6  "
set $ws7  "  7  "
set $ws8  "  8  "
set $ws9  "  9  "
set $ws10 "  10  "

#CAMBIAR ENTRE ESCRITORIOS
bindsym $mod+1 workspace $ws1 
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
 
#MOVER VENTANAS A ESCRITORIOS
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

#########AUDIO Y MEDIA PLAYER##########

#CONTROLES DE PULSEAUDIO
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +2% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -2% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

#CONTROLES MULTIMEDIA
bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

##########PROGRAMAS##########

#ABRIR FIREFOX
bindsym $mod+Shift+f exec firefox

#ABRIR THUNAR
bindsym $mod+Shift+t exec thunar

#ABRIR SPOTIFY
bindsym $mod+Shift+s exec alacritty -e ncspot

#ARBIR VIM
bindsym $mod+Shift+n exec alacritty -e nvim

#ARBIR SOCIAL ENGINEER TOOLKITS
bindsym $mod+Shift+h exec alacritty -e sudo setoolkit

#ABRIR VICUAL STUDIO CODE
bindsym $mod+Shift+v exec alacritty -e code

#ABRIR TERMINAL
bindsym $mod+Return exec alacritty

#ABRIR PYCHARM
bindsym $mod+Shift+p exec .local/myapps/pycharm-community-2021.1.2/bin/pycharm.sh

#ABRIR ROFI
bindsym $mod+x exec rofi -show drun

#########VENTANAS##########

#TITULO
title_align center

#COLOR DE LAS VENTANAS
#                              	Border  Background Text    Indicator
client.focused              	#a3be8c #a3be8c    #ffffff #b48ead
client.unfocused            	#81a1c1 #81a1c1    #ffffff #b48ead
client.focused_inactive   	#81a1c1 #81a1c1    #ffffff #b48ead
client.urgent               	#bf616a #bf616a    #ffffff #b48ead

#BORDES
default_border pixel          2px
default_floating_border pixel 2px

#########BRECHAS##########

#BRECHAS INTERNAS / EXTERNAS
gaps inner 10
gaps outer 10

#CONFIGURAR BRECHAS
set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#########BARRA i3##########
#i3 BARRA DE TAREAS

bar {
 	status_command i3blocks 
	position bottom
	height 28
    colors {
       background #1c202c
        statusline #ffffff
       separator #2e3440

#                          Border  Background  Text
        focused_workspace  #81a1c1 #81a1c1     #ffffff
        active_workspace   #1c202c #1c202c     #ffffff
        inactive_workspace #1c202c #1c202c     #ffffff
        urgent_workspace   #bf616a #bf616a     #ffffff
    }
}

#OCULTAR BARRA DE ESTADO
bindsym $mod+c bar mode toggle
